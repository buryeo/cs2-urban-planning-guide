# 2026-09-25 每日汇总

## 实际改动

- 浏览器原型加入 16 位灰度 PNG 高程图导入与尺寸校验；本地旧金山湾样例及设色预览存在时，启动后自动进入地形规划。新增 Mapzen 瓦片拼接脚本，生成文件留在忽略的 `prototype/local-samples/`。
- 新增旧金山湾节点预设、按水域和陡坡限制的片区生成，以及沿可通行陆地寻找主通道的候选走线；支持较短／缓坡偏好、必经点、线形柔化和急弯提示。
- 节点可选圆形或可旋转椭圆，影响半径下限改为 16；风向标可拖动，地图可切换仅看地形。同步更新说明和相关测试。

## 已体现的设计决定

- 高程图只提供地形依据；设色预览用于近似水陆判断。未识别桥位、航道、既有道路、资源或真实风场，跨水连接不凭空生成。外部玩家高程图只作预览，不套用旧金山湾规划预设。
- 第三方和生成的样例图留在本地，不提交到仓库。

## 待验证

- 在实际地图与游戏数据中核实海岸、水深、坡度、桥位、既有路网和资源位置；按道路等级校核曲率、容量与施工可行性。
- 全新检出仓库需运行高程图生成脚本后才会自动显示旧金山湾样例；脚本依赖 NumPy、Pillow。本次 JavaScript 测试 53 项通过，Python 测试因当前 `python3` 缺少 NumPy 未运行成功。
